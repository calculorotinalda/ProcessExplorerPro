<UserControl x:Class="ProcessExplorerPro.Views.ServicesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1000">
             
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0">
                <TextBlock Text="SERVIÇOS DO WINDOWS" Foreground="#5C62D6" FontSize="11" FontWeight="Bold"/>
                <TextBlock Text="Gestão de Serviços do Sistema" Foreground="#E1E3E6" FontSize="20" FontWeight="Bold" Margin="0,2,0,0"/>
            </StackPanel>
            
            <!-- Service Management Buttons -->
            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                <Button Command="{Binding StartCommand}" Background="#152A9D8F" BorderBrush="#2A9D8F" Margin="0,0,8,0" Padding="12,6">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock FontFamily="{StaticResource SegoeFluentFont}" Text="&#xE768;" Foreground="#2A9D8F" FontSize="12" Margin="0,0,6,0" VerticalAlignment="Center"/>
                        <TextBlock Text="Iniciar" Foreground="#2A9D8F" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
                           
                <Button Command="{Binding StopCommand}" Background="#15E63946" BorderBrush="#E63946" Margin="0,0,8,0" Padding="12,6">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock FontFamily="{StaticResource SegoeFluentFont}" Text="&#xE71A;" Foreground="#E63946" FontSize="12" Margin="0,0,6,0" VerticalAlignment="Center"/>
                        <TextBlock Text="Parar" Foreground="#E63946" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
                           
                <Button Command="{Binding RefreshCommand}" Padding="12,6">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock FontFamily="{StaticResource SegoeFluentFont}" Text="&#xE72C;" FontSize="12" Margin="0,0,6,0" VerticalAlignment="Center"/>
                        <TextBlock Text="Atualizar" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </Grid>

        <!-- Search & Control Area -->
        <Border Grid.Row="1" Background="#161821" BorderBrush="#232530" BorderThickness="1" CornerRadius="8" Padding="10" Margin="0,0,0,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="300" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <!-- Search Box -->
                <Grid Grid.Column="0">
                    <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" 
                             Tag="SearchBox"
                             Padding="10,6,30,6"
                             FontSize="12"/>
                    <!-- Search glass overlay -->
                    <TextBlock FontFamily="{StaticResource SegoeFluentFont}" Text="&#xE721;" Foreground="{StaticResource TextMutedBrush}" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,10,0" IsHitTestVisible="False"/>
                </Grid>
            </Grid>
        </Border>

        <!-- Services DataGrid -->
        <DataGrid Grid.Row="2" 
                  ItemsSource="{Binding DisplayedServices}" 
                  SelectedItem="{Binding SelectedService}"
                  AutoGenerateColumns="False" 
                  IsReadOnly="True" 
                  Background="#161821" 
                  BorderBrush="#232530" 
                  BorderThickness="1"
                  RowBackground="#161821"
                  AlternatingRowBackground="#191B26"
                  GridLinesVisibility="Horizontal"
                  HorizontalGridLinesBrush="#232530"
                  HeadersVisibility="Column"
                  SelectionMode="Single">

            <DataGrid.Resources>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="#232530"/>
                    <Setter Property="Foreground" Value="#848694"/>
                    <Setter Property="FontWeight" Value="SemiBold"/>
                    <Setter Property="Padding" Value="10,8"/>
                    <Setter Property="BorderThickness" Value="0,0,0,1"/>
                    <Setter Property="BorderBrush" Value="#232530"/>
                </Style>
                <Style TargetType="DataGridCell">
                    <Setter Property="Padding" Value="8,10"/>
                    <Setter Property="BorderThickness" Value="0"/>
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="Foreground" Value="#E1E3E6"/>
                </Style>
            </DataGrid.Resources>

            <DataGrid.Columns>
                <!-- Display Name -->
                <DataGridTemplateColumn Header="Nome de Exibição" Width="250" SortMemberPath="DisplayName">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock FontFamily="{StaticResource SegoeFluentFont}" Text="&#xE713;" Foreground="#5C62D6" FontSize="14" Margin="0,0,8,0" VerticalAlignment="Center"/>
                                <TextBlock Text="{Binding DisplayName}" FontWeight="SemiBold" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" Width="210"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <!-- Service Name -->
                <DataGridTextColumn Header="Nome Interno" Binding="{Binding Name}" Width="130" />
                
                <!-- PID -->
                <DataGridTextColumn Header="PID" Binding="{Binding Pid}" Width="70" />

                <!-- Status -->
                <DataGridTemplateColumn Header="Estado" Width="100" SortMemberPath="Status">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Border CornerRadius="12" Padding="8,2" HorizontalAlignment="Left">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Setter Property="Background" Value="#20E63946"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Status}" Value="Running">
                                                <Setter Property="Background" Value="#202A9D8F"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <TextBlock Text="{Binding Status}" FontSize="11" FontWeight="Bold">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Foreground" Value="#E63946"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Status}" Value="Running">
                                                    <Setter Property="Foreground" Value="#2A9D8F"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </Border>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <!-- Start Type -->
                <DataGridTextColumn Header="Tipo de Inicialização" Binding="{Binding StartType}" Width="120" />

                <!-- Log On As -->
                <DataGridTextColumn Header="Conta de Logon" Binding="{Binding LogOnAs}" Width="130" />

                <!-- Description -->
                <DataGridTextColumn Header="Descrição" Binding="{Binding Description}" Width="*" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
