<UserControl x:Class="ProcessExplorerPro.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1000">
             
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="CONFIGURAÇÕES" Foreground="#5C62D6" FontSize="11" FontWeight="Bold"/>
            <TextBlock Text="Ajustes da Aplicação" Foreground="#E1E3E6" FontSize="20" FontWeight="Bold" Margin="0,2,0,0"/>
        </StackPanel>

        <!-- Settings Cards Container -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel MaxWidth="800" HorizontalAlignment="Left">
                
                <!-- Personalization -->
                <TextBlock Text="Personalização" Foreground="#5C62D6" FontWeight="SemiBold" FontSize="14" Margin="0,0,0,8"/>
                <Border Background="#161821" BorderBrush="#232530" BorderThickness="1" CornerRadius="8" Padding="15,10" Margin="0,0,0,20">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" VerticalAlignment="Center">
                            <TextBlock Text="Tema Escuro" Foreground="#E1E3E6" FontWeight="SemiBold" FontSize="13"/>
                            <TextBlock Text="Alternar entre o tema claro e escuro para a interface gráfica." Foreground="#848694" FontSize="11" Margin="0,2,0,0"/>
                        </StackPanel>
                        <!-- Using custom ToggleSwitch style from App.xaml -->
                        <CheckBox Grid.Column="1" IsChecked="{Binding IsDarkTheme}" Style="{StaticResource ToggleSwitchStyle}" VerticalAlignment="Center"/>
                    </Grid>
                </Border>

                <!-- Monitoring Config -->
                <TextBlock Text="Monitorização" Foreground="#5C62D6" FontWeight="SemiBold" FontSize="14" Margin="0,0,0,8"/>
                <Border Background="#161821" BorderBrush="#232530" BorderThickness="1" CornerRadius="8" Padding="15,15" Margin="0,0,0,20">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" VerticalAlignment="Center">
                            <TextBlock Text="Frequência de Atualização" Foreground="#E1E3E6" FontWeight="SemiBold" FontSize="13"/>
                            <TextBlock Text="Define a velocidade de amostragem dos processos e gráficos." Foreground="#848694" FontSize="11" Margin="0,2,0,0"/>
                        </StackPanel>
                        <ComboBox Grid.Column="1" 
                                  ItemsSource="{Binding RefreshRateOptions}" 
                                  SelectedItem="{Binding RefreshRate}"
                                  Width="180" 
                                  VerticalAlignment="Center"/>
                    </Grid>
                </Border>

                <!-- Premium Features / Security -->
                <TextBlock Text="Segurança e Inteligência Artificial" Foreground="#5C62D6" FontWeight="SemiBold" FontSize="14" Margin="0,0,0,8"/>
                <Border Background="#161821" BorderBrush="#232530" BorderThickness="1" CornerRadius="8" Padding="15,10" Margin="0,0,0,20">
                    <StackPanel>
                        <!-- AI Analysis -->
                        <Grid Margin="0,5,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                <TextBlock Text="Deteção Inteligente de Ameaças (AI)" Foreground="#E1E3E6" FontWeight="SemiBold" FontSize="13"/>
                                <TextBlock Text="Executar heurísticas locais para identificar executáveis suspeitos ou caminhos anómalos." Foreground="#848694" FontSize="11" Margin="0,2,0,0"/>
                            </StackPanel>
                            <CheckBox Grid.Column="1" IsChecked="{Binding IsAiEnabled}" Style="{StaticResource ToggleSwitchStyle}" VerticalAlignment="Center"/>
                        </Grid>
                        
                        <Separator Background="#232530" Margin="0,5,0,5"/>

                        <!-- VirusTotal Integration -->
                        <Grid Margin="0,10,0,5">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                <TextBlock Text="Verificação de Assinaturas &amp; VirusTotal" Foreground="#E1E3E6" FontWeight="SemiBold" FontSize="13"/>
                                <TextBlock Text="Validar certificados digitais de processos e simular verificação na base de dados do VirusTotal." Foreground="#848694" FontSize="11" Margin="0,2,0,0"/>
                            </StackPanel>
                            <CheckBox Grid.Column="1" IsChecked="{Binding IsVirusTotalEnabled}" Style="{StaticResource ToggleSwitchStyle}" VerticalAlignment="Center"/>
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- About Card -->
                <TextBlock Text="Sobre o Software" Foreground="#5C62D6" FontWeight="SemiBold" FontSize="14" Margin="0,0,0,8"/>
                <Border Background="#161821" BorderBrush="#232530" BorderThickness="1" CornerRadius="8" Padding="20" Margin="0,0,0,20">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Border Grid.Column="0" Background="#2B5C62D6" CornerRadius="8" Width="55" Height="55" Margin="0,0,20,0" VerticalAlignment="Center">
                            <TextBlock FontFamily="{StaticResource SegoeFluentFont}" Text="&#xE727;" Foreground="#5C62D6" FontSize="26" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                            <TextBlock Text="Process Explorer Pro" Foreground="#E1E3E6" FontWeight="Bold" FontSize="16"/>
                            <TextBlock Text="Versão 1.0.0 (Build 9.0.2026)" Foreground="#848694" FontSize="11" Margin="0,2,0,0"/>
                            <TextBlock Text="© 2026 Google DeepMind Agentic Coding. Todos os direitos reservados." Foreground="#848694" FontSize="10" Margin="0,8,0,0"/>
                        </StackPanel>
                    </Grid>
                </Border>

            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
