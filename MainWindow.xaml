<Window x:Class="ProcessExplorerPro.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Process Explorer Pro" 
        Height="780" Width="1200"
        MinHeight="600" MinWidth="900"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent">

    <Window.Resources>
        <!-- Sidebar Radio Button Style -->
        <Style x:Key="SidebarRadioButtonStyle" TargetType="{x:Type RadioButton}">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#848694"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Padding" Value="15,10"/>
            <Setter Property="Margin" Value="0,2"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type RadioButton}">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="6" SnapsToDevicePixels="true">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <!-- Left Active indicator line -->
                                <Border x:Name="indicator" Grid.Column="0" Width="3" Height="16" Background="{StaticResource AccentBrush}" CornerRadius="1.5" HorizontalAlignment="Left" Margin="-15,0,0,0" Visibility="Collapsed"/>
                                <ContentPresenter Grid.Column="1" Margin="{TemplateBinding Padding}" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalAlignment="Center"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="true">
                                <Setter Property="Foreground" Value="White"/>
                                <Setter Property="Background" Value="#1A5C62D6"/>
                                <Setter TargetName="indicator" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="true">
                                <Setter Property="Foreground" Value="White"/>
                                <Setter Property="Background" Value="#0AFFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Custom TitleBar Button Style -->
        <Style x:Key="TitleBarButtonStyle" TargetType="{x:Type Button}">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#848694"/>
            <Setter Property="Width" Value="46"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border x:Name="border" Background="{TemplateBinding Background}" SnapsToDevicePixels="true">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="true">
                                <Setter Property="Background" Value="#15FFFFFF"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="true">
                                <Setter Property="Background" Value="#30FFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="TitleBarCloseButtonStyle" TargetType="{x:Type Button}" BasedOn="{StaticResource TitleBarButtonStyle}">
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="true">
                    <Setter Property="Background" Value="#E81123"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <!-- Rounded Border Frame -->
    <Border Background="{StaticResource BackgroundBrush}" 
            BorderBrush="{StaticResource BorderBrush}" 
            BorderThickness="1" 
            CornerRadius="8" 
            ClipToBounds="True">
        
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!-- Custom TitleBar -->
            <Grid Grid.Row="0" x:Name="TitleBarGrid" Height="32" Background="#0A0B10" MouseDown="TitleBarGrid_MouseDown">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <!-- Left info -->
                <StackPanel Grid.Column="0" Orientation="Horizontal" Margin="15,0,0,0" VerticalAlignment="Center">
                    <!-- App Logo Font Char -->
                    <TextBlock FontFamily="{StaticResource SegoeFluentFont}" Text="&#xE7F1;" Foreground="#5C62D6" FontSize="14" VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <TextBlock Text="Process Explorer Pro" Foreground="#E1E3E6" FontSize="12" FontWeight="SemiBold" VerticalAlignment="Center"/>
                    
                    <Border Background="#2B5C62D6" CornerRadius="4" Padding="6,2" VerticalAlignment="Center" Margin="10,0,0,0">
                        <TextBlock Text="PRO" Foreground="#5C62D6" FontSize="9" FontWeight="Bold"/>
                    </Border>
                </StackPanel>

                <!-- Right Window State Controls -->
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button Style="{StaticResource TitleBarButtonStyle}" Click="Minimize_Click">
                        <TextBlock FontFamily="{StaticResource SegoeFluentFont}" Text="&#xE921;" FontSize="10"/>
                    </Button>
                    <Button Style="{StaticResource TitleBarButtonStyle}" Click="Maximize_Click">
                        <TextBlock FontFamily="{StaticResource SegoeFluentFont}" Text="&#xE922;" FontSize="10"/>
                    </Button>
                    <Button Style="{StaticResource TitleBarCloseButtonStyle}" Click="Close_Click">
                        <TextBlock FontFamily="{StaticResource SegoeFluentFont}" Text="&#xE8BB;" FontSize="10"/>
                    </Button>
                </StackPanel>
            </Grid>

            <!-- Main Layout Body -->
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="220" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <!-- Left Navigation Sidebar -->
                <Grid Grid.Column="0" Background="#0C0D13">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <!-- Sidebar Header Info -->
                    <StackPanel Grid.Row="0" Margin="20,20,20,10">
                        <TextBlock Text="MENU DE MONITOR" Foreground="#5C62D6" FontSize="9" FontWeight="Bold"/>
                    </StackPanel>

                    <!-- Nav items -->
                    <StackPanel Grid.Row="1" Margin="10,0">
                        <RadioButton Tag="Dashboard" Click="NavigationButton_Click" IsChecked="True" Style="{StaticResource SidebarRadioButtonStyle}">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock FontFamily="{StaticResource SegoeFluentFont}" Text="&#xE9D2;" VerticalAlignment="Center" Margin="0,0,12,0" FontSize="14"/>
                                <TextBlock Text="Dashboard" VerticalAlignment="Center"/>
                            </StackPanel>
                        </RadioButton>

                        <RadioButton Tag="Processes" Click="NavigationButton_Click" Style="{StaticResource SidebarRadioButtonStyle}">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock FontFamily="{StaticResource SegoeFluentFont}" Text="&#xE71D;" VerticalAlignment="Center" Margin="0,0,12,0" FontSize="14"/>
                                <TextBlock Text="Processos" VerticalAlignment="Center"/>
                            </StackPanel>
                        </RadioButton>

                        <RadioButton Tag="Services" Click="NavigationButton_Click" Style="{StaticResource SidebarRadioButtonStyle}">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock FontFamily="{StaticResource SegoeFluentFont}" Text="&#xE713;" VerticalAlignment="Center" Margin="0,0,12,0" FontSize="14"/>
                                <TextBlock Text="Serviços" VerticalAlignment="Center"/>
                            </StackPanel>
                        </RadioButton>

                        <RadioButton Tag="Network" Click="NavigationButton_Click" Style="{StaticResource SidebarRadioButtonStyle}">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock FontFamily="{StaticResource SegoeFluentFont}" Text="&#xE839;" VerticalAlignment="Center" Margin="0,0,12,0" FontSize="14"/>
                                <TextBlock Text="Rede" VerticalAlignment="Center"/>
                            </StackPanel>
                        </RadioButton>

                        <RadioButton Tag="Performance" Click="NavigationButton_Click" Style="{StaticResource SidebarRadioButtonStyle}">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock FontFamily="{StaticResource SegoeFluentFont}" Text="&#xE9D9;" VerticalAlignment="Center" Margin="0,0,12,0" FontSize="14"/>
                                <TextBlock Text="Desempenho" VerticalAlignment="Center"/>
                            </StackPanel>
                        </RadioButton>
                    </StackPanel>

                    <!-- Settings at Bottom -->
                    <StackPanel Grid.Row="2" Margin="10,20">
                        <RadioButton Tag="Settings" Click="NavigationButton_Click" Style="{StaticResource SidebarRadioButtonStyle}">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock FontFamily="{StaticResource SegoeFluentFont}" Text="&#xE713;" VerticalAlignment="Center" Margin="0,0,12,0" FontSize="14"/>
                                <TextBlock Text="Configurações" VerticalAlignment="Center"/>
                            </StackPanel>
                        </RadioButton>
                    </StackPanel>
                </Grid>

                <!-- Right Dynamic Content Area -->
                <Grid Grid.Column="1" Background="{StaticResource BackgroundBrush}" Margin="15">
                    <Border CornerRadius="8" 
                            Background="{StaticResource CardBrush}" 
                            BorderBrush="{StaticResource BorderBrush}" 
                            BorderThickness="1"
                            ClipToBounds="True">
                        <Frame x:Name="ContentFrame" NavigationUIVisibility="Hidden"/>
                    </Border>
                </Grid>
            </Grid>
        </Grid>
    </Border>
</Window>
